---
wave: 1
depends_on: []
files_modified:
  - src/components/MidiSettings.tsx
  - src/App.tsx
autonomous: true
---

# Plan: 04-03 Global Dashboard Consolidation

Finalize the "Swiss/Minimalist" visual overhaul by updating global components and the application shell. Ensure all persistent UI elements (MIDI settings, loading states) use the new design tokens.

## Tasks

<task id="refactor-midi-settings">
Update `src/components/MidiSettings.tsx`:
- Replace hardcoded `purple-500` styles with `var(--accent)` and theme variables.
- Use `glass-panel` utility for the dropdown.
- Simplify animations and borders to match the new minimalist aesthetic.
</task>

<task id="update-loading-states">
Update `src/App.tsx`:
- Replace the simple text fallbacks (`Loading...`) with a unified `LoadingScreen` component or a styled spinner that matches the new theme.
- Ensure the root layout background is consistent (`var(--bg-app)`).
</task>

## Verification
- MIDI Settings menu looks consistent with the Sidebar.
- Loading a lazy-loaded module shows a proper loading indicator.
- No "purple" artifacts remain in the global shell; everything follows the CSS variables.
