---
wave: 1
depends_on: []
files_modified:
  - src/modules/ChordLab/ChordLab.tsx
  - src/modules/ChordLab/components/ChordLabHUD.tsx
  - src/components/ChordLabDashboard.tsx
autonomous: true
---

# Plan: 04-02 Refactor ChordLab UI for Minimalist Dashboard

Reorganize the `ChordLab` module to follow the new Swiss/Minimalist design system. The goal is to maximize the workspace (Progression Builder) and move controls to a streamlined HUD and sidebar.

## Tasks

<task id="create-hud">
Create `src/modules/ChordLab/components/ChordLabHUD.tsx`:
- A minimalist top bar containing:
  - Key/Scale selectors (compact).
  - Transport controls (Play/Stop, BPM).
  - Style selector.
  - Toggle buttons for Mixer, Tips, Export.
- Use `lucide-react` icons and `glass-panel` aesthetics.
</task>

<task id="refactor-dashboard-component">
Update `src/components/ChordLabDashboard.tsx`:
- Remove the `Controls` component (since it moves to HUD).
- Focus this component on rendering the layout of:
  - **ProgressionBuilder** (Main center).
  - **PianoKeyboard** (Bottom or collapsible).
  - **Structure Tools** (Sidebar or context panel).
- Remove the redundant wrapper divs to reduce nesting.
</task>

<task id="refactor-chordlab-entry">
Update `src/modules/ChordLab/ChordLab.tsx`:
- Implement the "Shell" layout:
  - **Left Sidebar**: `SmartLibrary` (move from Dashboard to here).
  - **Top Bar**: `ChordLabHUD`.
  - **Main Content**: `ChordLabDashboard` (Builder + Piano).
- Ensure full-height layout (`h-full`) to avoid double scrolling.
</task>

## Verification
- ChordLab loads with the new layout.
- Library is accessible on the left.
- Controls are available in the top bar.
- The progression builder has more screen space.
