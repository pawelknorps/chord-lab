---
wave: 1
depends_on: []
files_modified:
  - src/modules/FunctionalEarTraining/FunctionalEarTraining.tsx
  - src/modules/FunctionalEarTraining/components/HUD.tsx
  - src/modules/FunctionalEarTraining/components/LevelSelector.tsx
autonomous: true
---

# Plan: 03-02-01 Refactor Ear Training UI for Minimalist Dashboard

Update the `FunctionalEarTraining` module to align with the new Swiss/Minimalist design system. Focus on reducing visual clutter, using sidebar navigation for levels, and creating a focused "Work Area" for exercises.

## Tasks

<task id="fet-shell-update">
Update `src/modules/FunctionalEarTraining/FunctionalEarTraining.tsx`:
- Remove the top-heavy header and large "hero" sections.
- Move level selection to a cleaner side-menu or tab system (if not using global sidebar).
- Use `glass-panel` and `text-swiss-xl` utilities from `index.css`.
</task>

<task id="fet-components-update">
Refactor internal components (`HUD`, `LevelSelector` if kept) to be more subtle:
- HUD should be thin, possibly docked at the bottom or top-right.
- Icons should be small and functional (Lucide React).
- Colors should be functional (accent vs muted) rather than decorative gradients.
</task>

## Verification
- Module loads within the global dashboard without double-scrolling.
- Typography matches the global `Inter` font stack.
- "Start" and interaction buttons use the new minimalist button styles.
