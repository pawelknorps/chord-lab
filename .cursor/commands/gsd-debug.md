# GSD: Debug

Run a systematic investigation: reproduce the issue, isolate cause (code path, config, data), propose a minimal fix, and verify. Use the codebase (and `.planning/codebase/` if helpful). Document findings and any STATE.md or checkpoint updates if the user wants them recorded.
